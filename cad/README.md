# CAD

Enclosure sources go here. Target: 80 × 40 × 20 mm PETG body + separate TPU/PETG
clip piece with a Lamy-style spring blade (see `reference docs/reflux sentinel
complete project description.pdf` for the full design intent).

Files to add:

- `enclosure_body.f3d` / `.stl`
- `clip.f3d` / `.stl`
- Print settings (0.2 mm layer, 4 walls, 25 % infill)

Not yet started — this is scoped after firmware + webapp are validated on the
physical Pi.
